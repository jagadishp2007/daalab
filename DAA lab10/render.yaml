services:
  - type: web
    name: quicksort-comparison
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 30
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
